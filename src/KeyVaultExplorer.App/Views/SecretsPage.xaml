<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="KeyVaultExplorer.App.Views.SecretsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:KeyVaultExplorer.App.Models"
    xmlns:viewModels="clr-namespace:KeyVaultExplorer.App.ViewModels"
    x:DataType="viewModels:SecretsViewModel"
    Title="Secrets">

    <Grid
        Padding="24"
        RowDefinitions="Auto,Auto,Auto,*"
        RowSpacing="16">

        <VerticalStackLayout Grid.Row="0" Spacing="4">
            <Label
                FontSize="28"
                SemanticProperties.HeadingLevel="Level1"
                Text="{Binding HeaderTitle}" />
            <Label
                Text="Search secret names and open a secret to inspect versions and values."
                TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}" />
        </VerticalStackLayout>

        <Border Grid.Row="1" Padding="16">
            <VerticalStackLayout Spacing="8">
                <Label
                    Text="{Binding StatusMessage}"
                    IsVisible="{Binding HasStatus}" />
                <Label
                    Text="{Binding ErrorMessage}"
                    IsVisible="{Binding HasError}"
                    TextColor="{StaticResource Magenta}" />
            </VerticalStackLayout>
        </Border>

        <Grid
            Grid.Row="2"
            ColumnDefinitions="*,Auto"
            ColumnSpacing="12">
            <SearchBar
                Grid.Column="0"
                Placeholder="Search secret names"
                Text="{Binding SearchText}" />
            <Button
                Grid.Column="1"
                Command="{Binding RefreshCommand}"
                Text="Refresh" />
        </Grid>

        <RefreshView
            Grid.Row="3"
            Command="{Binding RefreshCommand}"
            IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding Secrets}">
                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="0,32" Spacing="8">
                        <Label
                            HorizontalTextAlignment="Center"
                            Text="No secrets found." />
                        <Label
                            HorizontalTextAlignment="Center"
                            Text="The vault may be empty or your account may not have permission."
                            TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:SecretItem">
                        <Border Margin="0,0,0,12" Padding="16">
                            <Grid
                                ColumnDefinitions="*,Auto"
                                RowDefinitions="Auto,Auto"
                                RowSpacing="6">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="18"
                                    Text="{Binding Name}" />
                                <Button
                                    Grid.Column="1"
                                    Grid.RowSpan="2"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenSecretCommand}"
                                    CommandParameter="{Binding .}"
                                    Text="Details" />
                                <HorizontalStackLayout Grid.Row="1" Spacing="12">
                                    <Label Text="{Binding UpdatedOn, StringFormat='Updated: {0:u}'}" />
                                    <Label Text="{Binding Enabled, StringFormat='Enabled: {0}'}" />
                                </HorizontalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
